书中第 $506$ 页的“【实例】用 map 统计字符串出现的次数”的代码中，有三处是这样的：

```cpp
scanf(“%s”, str);//第 4 行
scanf(“%s”, str);//第 7 行
printf(“%d\n”, h[str])//第 9 行
```

出现了代码中不该出现的中文标点，导致这个程序编译会失败。应该为：

```cpp
scanf("%s", str);//第 4 行
scanf("%s", str);//第 7 行
printf("%d\n", h[str])//第 9 行
```
